#!/bin/bash
# Chezmoi re-runs this script whenever opencode.json changes (model list updated).
# config hash: {{ include "dot_config/opencode/opencode.json" | sha256sum }}

set -euo pipefail
DOTFILES="$CHEZMOI_SOURCE_DIR/.."
bash "$DOTFILES/install/opencode.sh"
