#!/bin/bash
# Chezmoi re-runs this script whenever the agent-skills list or installer changes.
# skills list hash: {{ include "../packages/agent-skills.txt" | sha256sum }}
# installer hash: {{ include "../install/skills-sync.sh" | sha256sum }}

set -euo pipefail
DOTFILES="$CHEZMOI_SOURCE_DIR/.."
bash "$DOTFILES/install/skills-sync.sh"
