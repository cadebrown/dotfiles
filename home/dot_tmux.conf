# Let programs inside local or remote tmux sessions copy through OSC 52.
# Ghostty's SSH integration installs xterm-ghostty terminfo on remote hosts.
set -s set-clipboard on
set -as terminal-features ',xterm-ghostty:clipboard'
