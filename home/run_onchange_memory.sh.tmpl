#!/bin/bash
# Chezmoi re-runs this script whenever the memory stack's definition changes —
# the installer itself or the shared MCP list (qmd endpoint lives there).
# memory script hash: {{ include "../install/memory.sh" | sha256sum }}
# mcp list hash: {{ include "../packages/mcp-servers.txt" | sha256sum }}

set -euo pipefail
DOTFILES="$CHEZMOI_SOURCE_DIR/.."
bash "$DOTFILES/install/memory.sh"
