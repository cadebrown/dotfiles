{{- $name  := or (env "CHEZMOI_NAME")  (promptStringOnce . "name"  "Display name") -}}
{{- $email := or (env "CHEZMOI_EMAIL") (promptStringOnce . "email" "Email") -}}

[data]
  name  = {{ $name  | quote }}
  email = {{ $email | quote }}
