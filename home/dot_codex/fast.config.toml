# ~/.codex/fast.config.toml — `codex -p fast`
# Delta-only overlay on the base config (Codex 0.134+ profile format).
#
# Quick-iteration profile: the fastest GPT-5.6 tier at low reasoning effort.
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
plan_mode_reasoning_effort = "medium"
