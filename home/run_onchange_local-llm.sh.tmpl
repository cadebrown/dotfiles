#!/bin/bash
# Chezmoi rechecks local inference when its packages or installer change.
# full pip hash: {{ include "../packages/pip-full.txt" | sha256sum }}
# installer hash: {{ include "../install/local-llm.sh" | sha256sum }}

set -euo pipefail
DOTFILES="$CHEZMOI_SOURCE_DIR/.."
bash "$DOTFILES/install/local-llm.sh"
