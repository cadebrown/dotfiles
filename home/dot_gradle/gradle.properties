# Reuse task outputs and the JVM across local Java/Kotlin/Android builds.
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.parallel=true
# Configuration caching remains project-owned: plugin compatibility varies.
