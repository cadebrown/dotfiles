#!/bin/bash
# Chezmoi re-runs this script whenever cargo.txt changes.
# cargo.txt hash: {{ include "../packages/cargo.txt" | sha256sum }}

set -euo pipefail
DOTFILES="$CHEZMOI_SOURCE_DIR/.."
bash "$DOTFILES/install/rust.sh"
