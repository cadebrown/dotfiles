Create audit.zsh, a zsh script that counts literal occurrences of "needle" in each .txt file under data and writes report.tsv, sorted by filename. Include zero counts and filenames with spaces. Run it and verify the report. Keep input files unchanged. The script must work when called from a different working directory. Keep the final answer to the total file and occurrence counts.
