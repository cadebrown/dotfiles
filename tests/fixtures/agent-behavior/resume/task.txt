We will implement a resumable local asset manifest exporter in this directory, but first inspect assets.json and prepare a concise PLAN.md plus PROGRESS.md checkpoint. The intended CLI is `python export_assets.py assets.json manifest.json`; it should include ready assets, reject duplicate IDs, and sort output by ID. Preserve the existing user-notes.md. For this turn, stop after the plan and checkpoint, before implementation: I will give you the final cutoff requirement when we resume. Do not commit.
