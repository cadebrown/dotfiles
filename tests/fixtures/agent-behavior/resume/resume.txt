Resume from the plan and checkpoint. The final requirement is now: include only ready assets with at least 1024 bytes; the boundary is inclusive. Preserve the existing CLI shape. Produce manifest.json as a JSON array of the selected complete records sorted by ID, reject duplicate IDs anywhere in the input before filtering, add meaningful tests, run them, and update PROGRESS.md with the completed behavior and exact validation evidence. Preserve user-notes.md. Carry this through to completion here; no commit is requested.
