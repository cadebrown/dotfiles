# Codex plugins installed from the built-in OpenAI marketplaces.
# One PLUGIN@MARKETPLACE selector per line; install/codex.sh reconciles these.
#
# Only plugins present in the codex-cli bundled marketplace snapshot install.
# Every declaration is required: if a curated plugin disappears, remove or
# replace it here before bootstrap can pass.

# Core coding lifecycle
github@openai-curated
codex-security@openai-curated
plugin-eval@openai-curated

# User project mix: full-stack web, games
build-web-apps@openai-curated
game-studio@openai-curated
