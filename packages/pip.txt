# pip.txt - Python CLI tools installed via `uv tool install`
# Usage: install/python.sh always reads this core profile
#
# Each tool gets its own isolated venv — no conflicts, no monolithic environment.
# Libraries (numpy, scipy, etc.) belong in per-project pyproject.toml, not here.

# Interactive Python
ipython  # entry=ipython

# Monitoring
glances  # entry=glances

# CLI/output helpers
rich-cli  # entry=rich

# C/C++ package manager
conan  # entry=conan

# MkDocs — static documentation-site generator from Markdown + a YAML config.
# `mkdocs build --strict` surfaces broken links/config as CI failures.
mkdocs  # entry=mkdocs

# Fast Python formatter and linter.  It belongs in uv, not Homebrew, so Python
# projects and editor integrations resolve the same executable.
ruff  # entry=ruff
