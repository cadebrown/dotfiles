# claude-plugins.txt - Claude Code plugins to install
# Usage: install/claude.sh reads this file
# Install manually: claude plugin install <name>

# LSP integrations
rust-analyzer-lsp
clangd-lsp
pyright-lsp
typescript-lsp

# Meta / workflow
claude-md-management
# Anthropic's plugin/skill-authoring reference: 7 sub-skills (skill-development,
# plugin-structure, hook-development, mcp-integration, agent-development,
# command-development, plugin-settings) — we ship our own skills/plugins.
plugin-dev

# Security review (Trail of Bits marketplace — added by install/claude.sh).
# Plugin-only: ships specialized review agents, can't be vendored as a skill.
c-review@trailofbits

# Codex bridge (OpenAI official): run the installed codex CLI as a delegate —
# /codex:review, /codex:adversarial-review (second-model review!), /codex:rescue,
# background job status/result/cancel. Uses ~/.openai.env or ChatGPT login.
codex@openai-codex

# MCP server authoring (Anthropic official): build-mcp-server/build-mcp-app/
# build-mcpb skills — we maintain MCP entries for two agents.
mcp-server-dev
# Hook authoring/generation (Anthropic official): /hookify commands — we ship
# hooks (chezmoi-guard, rtk) from this repo.
hookify
# Claude Agent SDK development (Anthropic official)
agent-sdk-dev

# Fuzzing + sanitizers from ToB's Testing Handbook (12 namespaced sub-skills:
# cargo-fuzz, libfuzzer, aflpp, libafl, address-sanitizer, harness-writing,
# fuzzing-obstacles, fuzzing-dictionary, coverage-analysis, ossfuzz, ruzzy,
# atheris). Plugin route keeps the names namespaced.
testing-handbook-skills@trailofbits

# Stateful DAP debugging for agents: the `dap` CLI daemonizes a Debug Adapter
# session (Python/Go/Node/Rust/C++ backends) so debugging happens via plain
# Bash — no TTY, no tmux. Binary: brew AlmogBaku/tap/dap (Brewfile).
debugging-code@debug-skill-marketplace

# Lean 4 theorem-proving workflows (cameronfreer, 326★): formalize/prove/
# autoprove/disprove/golf with a plan→work→checkpoint→review cycle engine,
# budget enforcement, and proof-immutability guardrails. Plugin (not a bare
# skill): ships commands + hooks + subagents. NB: plugins are mirrored in
# THREE places — this file, _MARKETPLACES in install/claude.sh (third-party
# marketplaces only), and enabledPlugins/extraKnownMarketplaces in
# home/dot_claude/settings.json. Update all three.
lean4@lean4-skills
